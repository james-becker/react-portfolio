var React = require('react');
var ReactRouter = require('react-router');
var Link = ReactRouter.Link;

var Footer = React.createClass({
  render: function() {
    return (
      <div class="footer">Created with React.</div>
    )
  }
});

module.exports = Footer;
